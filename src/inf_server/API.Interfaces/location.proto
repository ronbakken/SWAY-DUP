syntax = "proto3";

package api;
option csharp_namespace = "API.Interfaces";

// To use in calls when no data is needed
message LocationDto {
    string name = 1;
    string description = 2;

    double latitude = 3;
    double longitude = 4;
}
